[phases.setup]
aptPkgs = ["python3", "python3-pip", "python3-dev", "libpq-dev"]

[phases.install]
cmds = [
    "pip3 install --break-system-packages -r requirements.txt"
]

[start]
cmd = "cd src && python3 app.py"
